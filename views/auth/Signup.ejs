<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('../includes/head.ejs',{title:"CAR-HAWA | Create Account"}) %>
        <link rel="stylesheet" href="css/form.css">
</head>

<body>
    <main>
        <%- include('../includes/header.ejs') %>
            <div class="form-div">
                <form action="/signup" method="post">
                    <% if (error) { %>
                        <p class="user-message user-message--error">
                            <%= error %>
                        </p>
                        <% } %>
                            <h3>Create an account.</h3>
                            <p><label for="email">Email</label>
                                <input type="email" id="email" name="email" required value="<%= initialInput.email %>">
                            </p>
                            <p><label for="name">Name</label>
                                <input type="text" id="name" name="name" required value="<%= initialInput.name %>">
                            </p>
                            <p><label for="phone">Phone</label>
                                <input type="text" id="phone" name="phone" required value="<%= initialInput.phone %>">
                            </p>
                            <p><label for="username">Username</label>
                                <input type="text" id="username" name="username" required
                                    value="<%= initialInput.username %>">
                            </p>
                            <p><label for="location">Location</label>
                                <select name="location" id="location" required>
                                    <option value="Eldoret">Eldoret</option>
                                    <option value="Nakuru">Nakuru</option>
                                    <option value="Nairobi">Nairobi</option>
                                    <option value="Kisumu">Kisumu</option>
                                    <option value="Mombasa">Mombasa</option>
                                </select>
                            </p>
                            <p><label for="password">Password</label>
                                <input type="password" name="password" id="password" required
                                    value="<%= initialInput.password %>">
                            </p>
                            <p>
                                <label for="confirmPassword">Confirm Password</label>
                                <input type="password" name="confirmPassword" id="confirmPassword" required
                                    value="<%= initialInput.confirmPassword %>">
                            </p>
                            <div class="button-div">
                                <p><button class="btn btn-alt" type="reset">Clear</button></p>
                                <p><button class="btn" type="submit">SignUp</button></p>

                            </div>
                            <a href="/login">Already has an account? Login</a>
                </form>
            </div>
    </main>
</body>

</html>