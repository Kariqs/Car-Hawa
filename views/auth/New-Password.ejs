<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('../includes/head.ejs',{title:"CAR-HAWA | New Password"}) %>
        <link rel="stylesheet" href="css/form.css">
</head>

<body>
    <main>
        <%- include('../includes/header.ejs') %>
            <div class="form-div">
                <form action="/new-password" method="post">
                    <% if (error) { %>
                        <p class="user-message user-message--error">
                            <%= error%>
                        </p>
                        <% } %>
                            <h3>Enter your new password.</h3>
                            <p> <input type="hidden" name="userId" id="userId" value="<%= userId %>"></p>
                            <p> <input type="hidden" name="passwordToken" id="passwordToken"
                                    value="<%= passwordToken %>"></p>
                            <p><label for="password">Password</label>
                                <input type="password" id="password" name="password" required>
                            </p>
                            <div class="button-div">
                                <p><button class="btn" type="submit">Reset.</button></p>
                            </div>
                </form>
            </div>
    </main>
</body>

</html>